spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3307

security:
  signing-key: "test-local-key"

junit:
  jupiter:
    execution:
      parallel:
        enabled: true
        mode:
          default: concurrent
